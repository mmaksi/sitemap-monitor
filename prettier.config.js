module.exports = {
    printWidth: 80,             // Wrap lines at 80 characters
    tabWidth: 2,                // Set indentation to 2 spaces
    useTabs: false,             // Use spaces instead of tabs
    semi: true,                 // End statements with semicolons
    singleQuote: true,          // Use single quotes instead of double quotes
    trailingComma: 'es5',       // Trailing commas where valid in ES5 (objects, arrays, etc.)
    bracketSpacing: true,       // Space between brackets in object literals
    arrowParens: 'always',      // Always include parens for arrow functions
    proseWrap: 'preserve',      // Wrap markdown text as-is
    endOfLine: 'lf',            // Line endings set to LF
};
